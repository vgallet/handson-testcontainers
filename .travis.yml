language: node_js
node_js:
  - "stable"

cache:
  directories:
    - "node_modules"

#before_install:
#  - npm install --bin-links

jobs:
  include:
    - stage: build github page
      node_js: "stable"
      script:
        - ./deploy.sh
#        - npm run build

